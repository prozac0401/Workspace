# 업무 책갈피 공개 MSI 수명주기·데이터 보존

기록일: 2026-09-27 · 시험: 2026-09-26~27 · 상태: 기본 설치 경로 50체크포인트 PASS, 실제 앱 입력 검증은 남음

## 대상과 범위

- 공개 WorkBookmark 0.2.3 MSI: 75,442,921 bytes, SHA-256 `3424a44367e7e7c1e1483d359c595dd568d2368cea48b61e83e6b63c65eca714`. 로컬 파일과 GitHub 자산 digest 일치.
- 이전 0.2.2 MSI: SHA-256 `0f4aab1d57018767d5046d2b51a7189aa27da944aa4b6b96146f94a9264a3f6d`.
- Windows 11 x64 10.0.22631, 일반 사용자. 기존 MSI·기본 설치 폴더·앱 프로세스·시작 메뉴/시작프로그램 바로가기 없음 확인 후 실행.
- 기존 포터블 폴더와 사용자 데이터는 유지했다. 앱을 자동 실행하지 않는 `/qn` 설치로 진행하고 DB·설정은 내용 출력 없이 파일 해시만 비교했다.

## 최종 결과

`scripts/test-workbookmark-installer.ps1`의 기본 설치 경로 시험에서 50개 체크포인트가 모두 통과했다.

| 시험 | 결과 |
|---|---|
| 공개 0.2.3 신규 설치·제거 | 두 MSI 작업 종료0, 실행 파일·시작 메뉴 확인, 제거 후 실행 파일·시작프로그램·제품 등록 없음 |
| 0.2.2→0.2.3 | 두 작업 종료0, 최종 0.2.3 제품 하나만 설치 상태5로 등록 |
| 0.2.3 복구·제거 | 종료0, 기본 경로 실행 파일·바로가기·등록 제거 |
| 데이터 보존 | 각 단계에서 bookmarks.db·settings.json SHA-256와 파일 존재 여부 동일 |
| 시작프로그램 선택 | 기존 Preferences 값 불변, 업데이트 전후 바로가기 존재 여부 동일, 제거 후 바로가기 없음 |
| 무인 설치 | 단계마다 WorkBookmark 앱 프로세스 없음 확인 |

## 초기 실패와 제한

처음 작성한 시험의 단일 HKCU Uninstall 경로 조회는 MSI의 실제 설치와 일치하지 않았다. Windows Installer의 [RelatedProducts API](https://learn.microsoft.com/en-us/windows/win32/msi/installer-relatedproducts)로 동일 UpgradeCode의 제품을 열거하고 VersionString·ProductState를 확인하도록 바꿨다. 단순히 등록 검사를 생략하지 않았다.

artifacts 아래에 INSTALLFOLDER를 지정한 초기 시험에서는 복구·제거 후 해당 사용자 지정 경로에 실행 파일이 남았다. 기본 경로 시험의 PASS로 이 실패를 덮지 않는다. 현재 공개 설치 화면은 설치 경로 변경 기능을 제공하지 않는다. 별도 경로 설치가 필요하면 경로 보존/복구 동작을 추가 조사·수정해야 한다. 초기 시험 산출물은 로컬 증거로 남겼다.

실제 앱을 실행하지 않았으므로 한글 IME, 창 전환 자동 저장, 저장 실패 초안, 스티커 위치·여러 모니터, 작업 재개, 실행 중 제거, 재로그인·재부팅은 NOT_RUN이다. DB 해시 보존은 이 UI 시험과 구분한다. 기존 209개 Desktop 회귀검사도 이번 패키지 실기 결과로 옮겨 쓰지 않는다.

진단·MSI 로그는 로컬 artifacts/audit/backlog-20260926과 작업 트리 artifacts/bookmark-lifecycle-*에만 보관한다. 사용자 DB·설정·로그 원문은 Git이나 공개 사이트에 포함하지 않는다. 제품 소스·공개 MSI·별도 BookMark 저장소는 변경하지 않았다. 설치본은 시험 후 제거했으며 기존 포터블 앱과 데이터는 남아 있다.
