# Codex 작업 지시 — Excel Smart List Compare 실제 Excel 검증

> 이 문서는 **Codex에 전달할 작업 지시문**이다. 테스트 실행 결과나 완성된 테스트 실행기는 아니다.
> 대상: `ExcelSmartListCompare_v0.2_Source.zip`을 풀어 얻은 프로젝트.
> 목표: Windows 데스크톱 Excel에서 빌드·기능·설치·재시작·재설치·제거를 실제로 실행하고, 확인된 증거와 함께 배포 후보를 만든다.

## 1. 먼저 읽을 파일과 현재 상태

프로젝트 루트에서 다음을 읽어 현재 구현을 이해한 뒤 실제 작업을 수행하라.

- `README.md`
- `docs/TEST_REPORT.md`
- `docs/CODEX_HANDOFF.md`
- `docs/ACCEPTANCE_TESTS.md`
- `Setup.ps1`, `Build_Release.cmd`, `Install.cmd`, `Uninstall.cmd`, `Test_Excel.cmd`
- `src/*_utf8.*`, `src/ThisWorkbook_events.txt`, `tests/`

현재 패키지의 기존 보고서는 Python 참조/정적 테스트 통과와 Windows 실기 미검증을 구분한다. 기존 로그를 새 Windows 실행의 증거로 재사용하지 말라. `SLC_TestAll`은 실제 Excel 통합 테스트 진입점이지만, 이것만 실행했다고 설치·재시작·GUI까지 통과한 것으로 처리하지 말라.

코드 리뷰나 계획 설명만으로 끝내지 말고, 가능한 검증은 실행하고 실제 실패를 수정한 뒤 재검증하라. 실행 권한이나 Excel이 없다면 해당 단계는 `BLOCKED`로 기록하고, 독립적으로 가능한 정적 검증과 테스트 도구 작성은 계속하라. 실행하지 않은 항목을 통과로 표시하지 말라.

## 2. 유지할 제품 요구사항

사용자의 사용법을 복잡하게 만드는 변경은 하지 말라.

- 모드·방향 선택 없이 첫 Selection을 기준으로 담고 다음 Selection과 비교한다.
- 가로↔세로 양방향, 부분 범위, 사각형, 단일 셀, Ctrl 다중 범위를 지원한다. 한 Selection 전체가 명단 하나이며, 두 열을 자동 분리하지 않는다.
- 같은 Excel 실행 세션에서 다른 통합문서도 지원한다. 독립 Excel 프로세스 간 공유 기능을 임의로 추가하지 않는다.
- 필터 제외 행 및 수동 숨김 행·열을 빼고 가시 셀만 비교한다. 겹친 범위의 같은 셀은 한 번만, 다른 셀의 같은 값은 중복으로 센다.
- 이메일은 @ 앞부분으로, 숫자/문자 숫자는 기존 정규화 규칙으로 비교한다. 텍스트 앞자리 0은 보존한다. 정렬과 무관하게 비교키별 개수도 비교한다.
- 기준은 독립 스냅샷이다. 기준 파일을 닫아도 유지하며, 경고 거절·실행 취소·오류 시 기준을 잃지 않는다.
- 큰 선택은 값 읽기 전 경고/상한 검사로 보호한다. 기본 경고 버튼은 ‘아니요’다. 성능 상수 변경은 실측 근거를 남긴다.
- 원본 값·수식·서식·필터를 바꾸지 않는다. 이전 결과 통합문서도 덮어쓰지 않는다.
- 전역 단축키를 자동 등록하지 않고 다른 추가 기능이나 메뉴를 초기화하지 않는다.

## 3. 실행 환경 사전 점검

### Windows/Excel/계정

Windows 네이티브 PowerShell 환경을 우선 사용하라. Codex 화면의 터미널만 PowerShell이라고 실제 에이전트도 Windows 네이티브라고 가정하지 말라. WSL에서 작업 중이면 승인된 Windows 측 실행 경로를 확인하고, 확인 없이 Linux 검증을 Excel 검증으로 대체하지 말라.

다음을 실제로 확인해 보고서에 남겨라.

- Windows 버전, PowerShell 버전, Codex의 실제 명령 실행 환경.
- 설치 대상 Windows 계정과 실제 실행 계정이 같은지 여부. 사용자 SID, 프로필, 세션을 비교하되 보고서 공개본에서는 식별자를 마스킹한다.
- `%LOCALAPPDATA%` 및 HKCU가 평소 Excel을 사용하는 계정의 것인지 여부.
- 데스크톱 Excel 설치 여부, 버전/빌드, **Excel 프로세스 자체의** 32/64비트. PowerShell 비트수로 Excel 비트수를 추정하지 않는다.
- Excel COM 생성과 종료가 가능한지, Office의 초기 실행/로그인/라이선스 대화상자가 남아 있지 않은지.
- 빌드용 VBA 프로젝트 접근과 실제 매크로 실행이 조직 정책상 허용되는지.

Codex 샌드박스는 다른 제한 계정이나 별도 데스크톱을 사용할 수 있다. 그 환경에서 설치가 성공했다는 것만으로 일반 사용자 Excel 설치 성공을 보고하지 말라. 실제 설치 대상 계정/세션에서 승인된 명령 실행이 필요하면, 해당 작업과 경로만 구체적으로 승인 요청하라. 전역 Full access나 관리자 실행을 기본 해결책으로 삼지 말라.

### 보안 경계

매크로 보안, 신뢰 위치, VBA 프로젝트 신뢰, PowerShell 실행 정책을 자동으로 변경하지 말라. `ExecutionPolicy Bypass`, 보안 경고 자동 수락, 전체 매크로 허용으로 검증을 통과시키지 말라. 정책 차단은 `BLOCKED_POLICY`로 남기고 승인된 개발/배포 환경을 안내하라.

COM으로 파일을 여는 테스트는 사용자 보안 정책을 존중한다. `AutomationSecurity` 기본값이 안전하다고 가정하지 말고 현재 코드의 `msoAutomationSecurityByUI` 의도를 유지하라.

## 4. 작업 안전과 원상복구

첫 변경 전에 제품 설치 여부/버전/파일 해시와 관련 추가 기능 등록 상태를 기록하라. 현재 제품이 설치되어 있다면 이전 제품 파일과 **제품 소유 항목만** 복구할 수 있는 백업을 확보하라. 다른 프로그램의 레지스트리 키 전체를 복원하거나 덮어쓰지 말라.

사용자의 열린 업무 문서는 저장·종료·수정하지 말라. 기존 Excel이 열려 있으면 현재 설치기의 안내 동작을 검증한 뒤, 실기 진행에 필요한 종료는 사용자에게 요청하라. `taskkill /IM EXCEL.EXE /F`, 전체 Excel 프로세스 종료, 타 추가 기능 비활성화는 금지한다.

기능 시험용 Excel은 테스트가 직접 만든 인스턴스를 사용하고 해당 인스턴스의 PID/창 핸들을 추적하라. 종료는 정상 `Quit`과 COM 참조 해제를 우선한다. 응답이 없으면 실패 상태와 소유 PID를 기록하고 무제한 대기를 막되, 사용자 프로세스를 강제로 종료하지 말라.

실제 명단이나 메일 주소 대신 합성 테스트 데이터만 사용한다. 로그/스크린샷은 테스트 창에 한정하고 사용자 문서나 다른 화면을 수집하지 말라.

기본 종료 상태는 **테스트 시작 전 상태로 복구**한다. 처음 미설치였다면 마지막은 미설치, 기존 설치가 있었다면 기존 상태로 복구한다. 검증용 새 버전을 사용자의 PC에 계속 설치해 두는 것은 별도 요청 없이는 하지 않는다. 복구 불가능한 오류는 숨기지 말고 잔여 파일·등록·프로세스를 정확히 보고하라.

## 5. 기존 실행 경로 재사용 및 자동화 보완

우선 기존 `Setup.ps1`과 VBA 테스트를 재사용하라. 다음은 기존 진입점의 직접 실행 형식이다. 실행하기 전에 소스를 확인하고, 쓰기 작업은 승인된 범위에서만 수행한다.

```powershell
powershell.exe -NoLogo -NoProfile -STA -File .\Setup.ps1 -Action Build
powershell.exe -NoLogo -NoProfile -STA -File .\Setup.ps1 -Action Test
powershell.exe -NoLogo -NoProfile -STA -File .\Setup.ps1 -Action Install
powershell.exe -NoLogo -NoProfile -STA -File .\Setup.ps1 -Action Uninstall
```

이 명령이 설치·제거 확인창까지 없애는 것은 아니다. `.cmd`의 `pause`, 확인창, 종료 코드 전달을 확인하라. 필요하면 테스트용 무대기 실행 옵션과 명확한 성공/실패/취소 종료 코드를 추가하되, 일반 사용자의 확인 UX는 유지한다. 테스트용 확인 생략 옵션이 필요할 경우 명시적으로 승인된 이 제품 설치/삭제에만 적용하고 기본값은 비활성화한다. 보안 경고 우회에 재사용하지 않는다.

PowerShell 파서로 문법을 확인하고, UTF-8 소스 수정 후에는 `python tests/export_ascii.py`로 VBA 가져오기 파일을 동기화하라. Python 테스트와 실제 Excel VBA 테스트를 모두 다시 실행하라. `.cmd` 자체의 사용자 설치 경로도 별도로 검증하라. PowerShell 직접 실행만 성공하고 `.cmd`는 실패하는 상태를 놓치지 말라.

## 6. 실제 Excel 기능 테스트

`Application.Run`으로 실제 `.xlam`의 `SLC_Version`, `SLC_TestAll`을 호출하고 실제 결과를 기록하라. `SLC_AttachUI`와 `SLC_UiReady`는 메뉴 등록 검사에 사용하되 화면 표시/클릭 성공의 증거로 대체하지 말라.

추가로 실제 선택을 만들고 일반 사용 진입점 `SLC_Run`을 거치는 흐름을 테스트하라. 테스트용 내부 함수에만 통과하도록 제품 경로를 우회하지 말라. 테스트 기대값은 제품 코드의 정규화 함수를 다시 호출해서 계산하지 말고 독립적으로 정의해 자기검증을 피하라.

필수 검증 그룹:

1. 가로↔세로 양방향, 사각형↔한 줄, 한 셀, 여러 영역/겹침, 전체 행/열의 사용 범위 제한.
2. 서로 다른 실제 파일 두 개를 같은 Excel 세션에서 열어 비교. 첫 파일 종료 이후 스냅샷 유지. 독립 Excel 프로세스 간 비공유 경계.
3. AutoFilter/Table 필터, 수동 숨김 행/열, 제목·합계행 제외, 제목 옆 일반 셀 보존, 전부 숨김/전부 빈칸/오류/병합 셀.
4. 숫자↔문자 숫자, 앞자리 0, 대소문자/공백/이메일 ID, 중복 개수 차이와 동일 목록.
5. 경고·상한의 직전/경계/초과값, 필터 조각 수, 텍스트 길이 상한, 경고 ‘아니요’, 실제 실행 중 취소.
6. 성공/오류/취소 후 기준 및 Excel 설정 복원, 이전 결과 보존, 원본 미변경, 출력 `HasFormula=False` 확인.

UI 경고 테스트에서 자동으로 ‘예’만 누르고 경고 검사 통과로 보고하지 말라. 창의 메시지, 기본 버튼, 거절 동작을 각각 확인하라. 성능은 범위 검사·읽기·비교·출력과 전체 지연을 구분해 측정하고, 테스트 데이터/PC 환경과 함께 기록하라. 30초 보호는 체크포인트 기반이라는 기존 경계를 유지한다.

## 7. 설치·재시작·제거 E2E

가능하면 먼저 승인된 전용 테스트 계정 또는 Windows VM에서 수행한다. 실사용 계정에서도 검증할 때는 앞서 정의한 원상복구와 사용자 데이터 보호를 지킨다.

다음 전체 순서를 실행하고 각 단계별 종료 코드와 실제 상태를 확인하라.

```text
초기 상태 기록 → 빌드 → Release 패키지 설치
→ Excel 정상 시작으로 자동 로드 확인
→ 일반 기능/메뉴 검증 → 정상 종료
→ Excel 재시작으로 등록 지속성 확인
→ 재설치 → 재시작 후 메뉴 중복 없음 확인
→ 제거 → Excel 재시작
→ 자동 로드/자기 메뉴/누락 파일 경고 없음 확인
→ 재제거의 안전한 동작 확인 → 초기 상태 복구
```

**자동 로드 검사에서는 테스트 코드가 먼저 `.xlam`을 열거나 `SLC_AttachUI`를 호출하지 말라.** 이는 설치 결함을 숨긴다. 사용자가 평소 Excel을 여는 정상 경로와 COM 테스트 경로를 구분한다. Excel이 시작한 뒤 등록 경로·실제 로드·메뉴 상태를 독립적으로 관찰하라.

추가 항목:

- Excel 열림 상태에서 설치·제거가 사용자 Excel을 종료하지 않고 중단하는지.
- 설치/제거 동시 실행 시 제품 전용 잠금이 작동하는지.
- 설치 폴더에 합성 사용자 파일을 하나 추가한 뒤 제거하면 그 파일이 남는지.
- 다른 추가 기능 등록 및 메뉴, PERSONAL.XLSB, 전역 단축키가 보존되는지.
- 한국어/공백 경로에서 동작하는지. 실제로 보유한 Excel 비트수만 검증했다고 보고하고, 없는 32/64비트 환경은 미검증으로 둔다.
- 실패/롤백 시험은 전용 테스트 환경에서 제품 파일만 대상으로 수행한다. 회사 보안 정책을 바꿔 실패를 유발하지 않는다.
- 최종 사용자 패키지 설치에서 VBA 소스 가져오기 권한이 요구되지 않는지.

전체 레지스트리의 ‘변화 없음’으로 판정하지 말라. Excel 자체가 정상 실행 중 갱신하는 값과 이 제품이 만든 변경을 구분하고, 타 추가 기능의 경로/활성 상태 및 제품 소유 항목을 중심으로 검사한다.

## 8. 실제 화면과 UX 검증

COM 자동화는 실제 Excel의 기능을 실행하지만, API의 메뉴 등록 성공과 사람이 보는 메뉴·버튼의 사용성은 별개다.

현재 Codex 환경에서 실제 Excel 창을 관찰/조작할 도구가 제공되는지 확인하라. 사용 가능한 Windows UI Automation 또는 승인된 데스크톱 조작 도구가 있으면 셀·행·열 우클릭 메뉴, 추가 기능 탭, 선택→기준 담기→다른 파일 선택→비교, 경고 거절·취소를 실제로 조작한다. 가능하면 접근성 식별자를 사용하고, 고정 좌표나 무차별 Enter 입력에 의존하지 않는다.

UI 도구나 사용자 데스크톱 접근이 없으면 해당 항목은 `NEEDS_MANUAL`로 남기고 최소한의 수동 확인 절차를 작성하라. 내부 매크로 직접 호출 성공을 ‘버튼 클릭 검증 완료’라고 보고하지 말라. 테스트 창의 증거 스크린샷도 실제 촬영한 경우만 첨부한다.

## 9. 산출물과 완료 판정

다음을 생성 또는 갱신하라.

- 재실행 가능한 Windows 테스트 실행기와 합성 테스트 자료.
- `artifacts/windows-e2e/<run-id>/` 아래 실제 명령 출력·종료 코드·환경 요약·제품 관련 설치 전후 차이·성능 결과.
- `docs/WINDOWS_E2E_REPORT.md`: ID / 검증 계층 / 환경 / 기대값 / 실제값 / 상태 / 증거 경로.
- 상태 구분: `PASS`, `FAIL`, `BLOCKED_ENV`, `BLOCKED_POLICY`, `NEEDS_MANUAL`, `NOT_RUN`.
- 수정한 소스와 수정 이유, UTF-8/가져오기 소스 동기화 결과.
- 검증한 최종 소스로 만든 `Release/`와 배포 후보 ZIP, SHA-256 목록.

최종 빌드 이후 다시 빌드했다면 이전 바이너리의 검증 결과를 새 바이너리 결과로 재사용하지 말라. 실제 설치한 XLAM의 해시를 최종 배포본과 대조한다. 서명 또는 패키징으로 바이트가 바뀌었다면 최종 패키지 대상으로 필요한 로드/설치 스모크 검증을 재실행한다.

실기 핵심 항목이 실패/미실행이면 ‘검증 완료 배포본’이라고 하지 말고 ‘배포 후보’라고 명시한다. 한 PC의 성공을 모든 Excel 버전/회사 보안 정책/타사 추가 기능과의 무충돌 보장으로 확대하지 말라.

최종 보고에는 무엇을 실제 실행했는지, 무엇을 고쳤는지, 남은 검증과 차단 사유, 원상복구 결과, 사용자가 설치할 파일 위치를 분명히 적어라.

---

## 참고 근거

확인일: 2026-09-14. 다음은 작업 지시의 기술적 근거이며 테스트 성공 증거가 아니다.

- OpenAI Windows 실행 환경: https://developers.openai.com/codex/windows/windows-app
- OpenAI Windows sandbox 및 실행 계정 경계: https://developers.openai.com/codex/windows/windows-sandbox
- Excel 매크로 호출: https://learn.microsoft.com/en-us/office/vba/api/excel.application.run
- Excel 추가 기능 설치 상태: https://learn.microsoft.com/en-us/office/vba/api/excel.addin.installed
- Excel 자동화 보안: https://learn.microsoft.com/en-us/office/vba/api/excel.application.automationsecurity
- Office 비대화형 자동화·UI·사용자 세션 주의사항: https://learn.microsoft.com/en-us/office/client-developer/integration/considerations-unattended-automation-office-microsoft-365-for-unattended-rpa
- 프로젝트 근거: 원본 ZIP의 `Setup.ps1`, `src/modSLCMain_utf8.bas`, `README.md`, `docs/TEST_REPORT.md`, `docs/ACCEPTANCE_TESTS.md`, `docs/CODEX_HANDOFF.md`.
